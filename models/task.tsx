export type TaskType = {
  id: number,
  name: string,
  description: string,
  dueDate: string,
  priority: string,
}

export type ShowDropdwonType = {
  isCreacte: boolean,
  isUpdate: boolean,
}

export type ShowValidateType = {
  isCreacte: boolean,
  isUpdate: boolean,
}
